# Protein Language Models Usage Guide

Example prompts:

- "Generate ESM embeddings for these protein sequences and cluster them."
- "Score missense variants with a protein language model and keep residue numbering traceable."
- "Use embeddings as features for a protein function classifier."

The agent will document model checkpoint, pooling strategy, and compute needs.
